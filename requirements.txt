# FlexPosit MICRO 2026 AE — pinned Python deps
# Install order matters: torch first, then qtorch_plus (compiles against torch),
# then transformers/lm-eval.

# Torch stack — CUDA 11.8 build
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# Posit quantization backend
qtorch_plus==0.2.0

# Transformer models
transformers==4.45.2
tokenizers==0.20.1
accelerate==1.10.1
safetensors==0.4.5
sentencepiece==0.2.0
huggingface-hub==0.26.1

# Datasets (WikiText-2 + downstream)
datasets==3.0.2

# Downstream evaluation
lm_eval==0.4.9.1

# Scientific stack
numpy<2.0
scipy==1.15.3

# Build tools (needed by qtorch_plus JIT compile of C++/CUDA extensions)
ninja

# Utilities
tqdm
matplotlib
pandas
